---
extends: default

rules:
  indentation:
    spaces: 2
    indent-sequences: whatever  # 允许序列对齐灵活处理
  
  empty-lines:
    max: 3
  
  line-length:
    max: 160
  
  document-start: disable
  
  truthy:
    allowed-values: ['true', 'false', 'yes', 'no', 'on', 'off']
